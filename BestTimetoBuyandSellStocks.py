class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit =0
        buy =prices[0]

        while (price in prices):
            buy =min(pri)
                profit = max(profit, prices[r]-prices[l])
            else :
                r=l

        return profit      
